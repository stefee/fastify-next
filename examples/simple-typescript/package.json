{
  "name": "simple-typescript",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "next build",
    "start": "ts-node server.ts --swc",
    "dev": "NODE_ENV=development ts-node server.ts --swc | pino-pretty"
  },
  "dependencies": {
    "fastify-next": "file:../.."
  }
}
