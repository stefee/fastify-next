{
  "name": "simple-typescript",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "next build",
    "start": "ts-node server.ts --swc --transpileOnly",
    "dev": "NODE_ENV=development ts-node server.ts --swc --transpileOnly | pino-pretty"
  },
  "dependencies": {
    "fastify-next": "file:../.."
  }
}
